import { useState } from "react";
import { Search, Bug, Play, Upload, BrainCircuit } from "lucide-react";

export default function AICodeEditor() {
  const [code, setCode] = useState(`import React from "react";

export default function App() {
  return <h1>MENACE 2 SOCIETY</h1>;
}`);

  const [analysis, setAnalysis] = useState([
    "✔ React component detected",
    "✔ JSX syntax valid",
    "⚠ Missing routing system",
    "⚠ No error boundaries found",
    "✔ Render structure optimized"
  ]);

  return (
    <div className="min-h-screen bg-black text-white flex overflow-hidden">
      {/* Sidebar */}
      <aside className="w-72 bg-zinc-950 border-r border-zinc-800 p-4 flex flex-col gap-4">
        <div>
          <h1 className="text-2xl font-bold tracking-wide text-red-500">
            MENACE AI IDE
          </h1>
          <p className="text-zinc-400 text-sm mt-1">
            AI-powered cinematic development engine
          </p>
        </div>

        <div className="bg-zinc-900 rounded-2xl p-4 border border-zinc-800">
          <h2 className="font-semibold mb-3">AI Agents</h2>

          <div className="space-y-2 text-sm">
            <div className="bg-zinc-800 rounded-xl px-3 py-2 flex justify-between">
              <span>Code Assistant</span>
              <span className="text-green-400">ONLINE</span>
            </div>

            <div className="bg-zinc-800 rounded-xl px-3 py-2 flex justify-between">
              <span>Bug Fixer</span>
              <span className="text-green-400">ONLINE</span>
            </div>

            <div className="bg-zinc-800 rounded-xl px-3 py-2 flex justify-between">
              <span>AI Architect</span>
              <span className="text-yellow-400">IDLE</span>
            </div>

            <div className="bg-zinc-800 rounded-xl px-3 py-2 flex justify-between">
              <span>Realtime Engine</span>
              <span className="text-green-400">ONLINE</span>
            </div>
          </div>
        </div>

        <div className="bg-zinc-900 rounded-2xl p-4 border border-zinc-800 flex-1">
          <h2 className="font-semibold mb-3">Project Files</h2>

          <div className="space-y-2 text-sm text-zinc-300">
            <div className="bg-zinc-800 rounded-lg px-3 py-2">
              index.html
            </div>
            <div className="bg-zinc-800 rounded-lg px-3 py-2">
              App.jsx
            </div>
            <div className="bg-zinc-800 rounded-lg px-3 py-2">
              server.js
            </div>
            <div className="bg-zinc-800 rounded-lg px-3 py-2">
              aiEngine.js
            </div>
            <div className="bg-zinc-800 rounded-lg px-3 py-2">
              socket.js
            </div>
            <div className="bg-zinc-800 rounded-lg px-3 py-2">
              firebase.js
            </div>
          </div>
        </div>
      </aside>

      {/* Main Editor */}
      <main className="flex-1 flex flex-col">
        {/* Top Bar */}
        <header className="h-16 border-b border-zinc-800 bg-zinc-950 px-6 flex items-center justify-between">
          <div>
            <h2 className="font-semibold text-lg">
              cinematic-platform.jsx
            </h2>
          </div>

          <div className="flex gap-3">
            <button className="bg-red-600 hover:bg-red-500 transition px-4 py-2 rounded-xl text-sm font-medium">
              Run Project
            </button>

            <button className="bg-zinc-800 hover:bg-zinc-700 transition px-4 py-2 rounded-xl text-sm font-medium">
              Deploy
            </button>
          </div>
        </header>

        {/* Editor Area */}
        <div className="flex-1 grid grid-cols-3 overflow-hidden">
          {/* Code Editor */}
          <section className="col-span-2 bg-[#0d0d0d] p-6 overflow-auto border-r border-zinc-800">
            <div className="bg-zinc-950 rounded-2xl p-5 border border-zinc-800 min-h-full">
              <pre className="text-sm leading-7 text-zinc-300 whitespace-pre-wrap">
{`import React from "react";
import { motion } from "framer-motion";

export default function Hero() {
  return (
    <motion.div
      initial={{ opacity: 0 }}
      animate={{ opacity: 1 }}
      className="hero"
    >
      <h1>MENACE 2 SOCIETY</h1>
      <p>Cinematic AI Platform</p>
    </motion.div>
  );
}`}
              </pre>
            </div>
          </section>

          {/* AI Assistant */}
          <section className="bg-zinc-950 flex flex-col">
            <div className="border-b border-zinc-800 p-4">
              <h2 className="font-semibold text-lg">AI Coding Assistant</h2>
              <p className="text-zinc-400 text-sm mt-1">
                Generate, debug, optimize, and build full systems.
              </p>
            </div>

            <div className="flex-1 overflow-auto p-4 space-y-4">
              <div className="bg-zinc-900 rounded-2xl p-4 border border-zinc-800">
                <p className="text-sm text-zinc-300">
                  Suggested Optimization:
                </p>

                <div className="mt-3 bg-black rounded-xl p-3 text-xs text-green-400 font-mono">
                  Add lazy loading for motion comic assets.
                </div>
              </div>

              <div className="bg-zinc-900 rounded-2xl p-4 border border-zinc-800">
                <p className="text-sm text-zinc-300">
                  Bug Detection:
                </p>

                <div className="mt-3 bg-black rounded-xl p-3 text-xs text-red-400 font-mono">
                  Socket reconnect handling missing.
                </div>
              </div>

              <div className="bg-zinc-900 rounded-2xl p-4 border border-zinc-800">
                <p className="text-sm text-zinc-300">
                  AI Generated Feature:
                </p>

                <div className="mt-3 bg-black rounded-xl p-3 text-xs text-blue-400 font-mono">
                  Generated realtime creator session manager.
                </div>
              </div>
            </div>

            {/* Prompt Input */}
            <div className="border-t border-zinc-800 p-4">
              <div className="bg-zinc-900 rounded-2xl border border-zinc-800 p-3">
                <textarea
                  className="w-full bg-transparent outline-none resize-none text-sm text-zinc-300"
                  rows={4}
                  placeholder="Ask the AI to build systems, debug code, generate UI, or optimize performance..."
                />

                <div className="flex justify-end mt-3">
                  <button className="bg-red-600 hover:bg-red-500 transition px-5 py-2 rounded-xl text-sm font-semibold">
                    Generate
                  </button>
                </div>
              </div>
            </div>
          </section>
        </div>
      </main>
    </div>
  );
}
